{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/widget.js --minify --format=esm --bundle --outdir=src/dimbridge/static"
	},
	"dependencies": {
		"d3": "^7.9.0",
		"html2canvas": "^1.4.1",
		"jspdf": "^2.5.2",
		"regl": "^2.1.0"
	},
	"devDependencies": {
		"esbuild": "^0.23.0"
	}
}
